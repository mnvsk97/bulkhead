# Deep Agents Example

This uses `create_deep_agent()` with two trivial tools so failure handling is easy to observe.

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
OPENAI_API_KEY=... OPENAI_BASE_URL=http://localhost:5000/v1 python main.py
```
