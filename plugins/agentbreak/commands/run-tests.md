---
description: Run chaos tests against your agent using AgentBreak
allowed-tools: Read, Glob, Grep, Bash, Edit, Write, AskUserQuestion
---

Run chaos tests against an LLM agent or MCP server using AgentBreak.

Raw slash-command arguments:
`$ARGUMENTS`

Use the `agentbreak-run-tests` skill for the step-by-step workflow: configure, inspect, validate, serve, wire, run traffic, collect scorecard, produce report.
