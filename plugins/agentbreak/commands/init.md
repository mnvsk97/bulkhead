---
description: Initialize AgentBreak and analyze your agent codebase
allowed-tools: Read, Glob, Grep, Bash, Edit, Write, AskUserQuestion
---

Initialize AgentBreak for chaos testing.

Raw slash-command arguments:
`$ARGUMENTS`

Use the `agentbreak-init` skill for the full workflow: install check, init config, analyze codebase, and configure application.yaml + scenarios.yaml.
