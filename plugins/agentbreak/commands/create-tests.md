---
description: Generate chaos test scenarios for AgentBreak
allowed-tools: Read, Glob, Grep, Bash, Edit, Write, AskUserQuestion
---

Generate chaos test scenarios for AgentBreak.

Raw slash-command arguments:
`$ARGUMENTS`

Use the `agentbreak-create-tests` skill for the full schema reference, example scenarios, and validation workflow.
